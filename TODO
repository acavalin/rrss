- rss_mngr:
  - dump feed link (prompt xml/json => window.open)
  - perss "N" to go to the next unread visible folder
  - implement "forward url" function
  - edit configuration GUI (feeds.yml, config.yml)
    - CGF: feed comment key

- rss_dler:
  - items filtering:
    - CFG: reject/accept item by key+regexp matching
    - filters scripts folder (like accept option)
  - export feedlist to OPML (xml)
  - support enclosure? --> http://en.wikipedia.org/wiki/RSS_enclosure
