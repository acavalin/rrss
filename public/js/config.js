(function ($) { $(function () {
// -----------------------------------------------------------------------------
$.extend($, {
  config: {
    split_panels_h: ['25%', '75%'],
    split_panels_v: ['30%', '70%'],
    feeds_refresh_time: 5,  // minutes
  },//config
});
// -----------------------------------------------------------------------------
}); })(jQuery);
