source 'http://rubygems.org'

# NOTE: ruby-1.9.2-p290 doesn't have the "Psych is unable to parse an array of symbols" problem
# ruby '1.9.2'

gem 'sqlite3', '~>1.3.6'
gem 'thin',    '~>1.5.1'
gem 'sinatra', '~>1.4.3'

if RUBY_VERSION < '2'
  gem 'debugger'
else
  gem 'byebug'
end

gem 'sinatra-reloader', '~>1.0'
